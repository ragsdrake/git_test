# git_test

This repository contains sample code for a cross-platform Pomodoro timer app with a simple ranking system and animated pets. The app is built using Flutter and demonstrates how to store the number of completed timers locally using `shared_preferences`. Pets are rendered as 2D shapes drawn in code, inspired by Apple's clean aesthetic.

The main code resides in `pomodoro_app/lib/main.dart`.
